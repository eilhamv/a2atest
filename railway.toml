[build]
builder = "NIXPACKS"

[deploy]
startCommand = "cd email-agent && python __main__.py"
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10